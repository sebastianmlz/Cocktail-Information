<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/index.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container contenedor">
        <h1 class="title">The Cocktail</h1>

        <form class="formu p-4 row m-5" method="GET">
            <div class="mb-3">
                <label for="textID" class="form-label">Cocktail Name</label>
                <input type="text" class="form-control" id="textID" name="name">
            </div>
            <div class="mb-3">
                <label for="firstID" class="form-label">Cocktail by first letter</label>
                <input type="text" class="form-control" id="firstID" name="first">
            </div>
        
            <p>Search by : </p>
            <div class="butons">
                <button type="submit" class="btn btn-primary" style="max-width: 8rem;" formaction="/byName">Name</button>
                <button type="submit" class="btn btn-success" style="max-width: 8rem;" formaction="/byFirst">First Letter</button>
                <button type="submit" class="btn" style="max-width: 8rem; background-color: #c81; color: #fff;" formaction="/random">Random</button>
            </div>
        </form>
        <% if(locals.result) { %>
            <h2 style="color: #efefef;">Search Result</h2>
        <% } else {%>
            <h2 style="color: #efefef;"> <%= locals.secret %> </h2>
        <% } %>
    </div>

    <% if (locals.result) { %>
        <% if (Array.isArray(locals.result)) { %>
            <!-- Incluir parcial para arrays -->
            <%- include('parcials/byFirst.ejs') %>
        <% } else { %>
            <!-- Incluir parcial para objetos -->
            <%- include('parcials/byName.ejs') %>
        <% } %>
    <% } %>


</body>
</html>